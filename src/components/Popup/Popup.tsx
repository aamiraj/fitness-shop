import React from "react";

const Popup = ({
  orderPopup,
  handleOrderPopup,
}: {
  orderPopup: boolean;
  handleOrderPopup: () => void;
}) => {
  return <div>Popup</div>;
};

export default Popup;
